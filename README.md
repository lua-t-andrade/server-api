# server-api

This is my first attempt at back-end development. A simple server API in Clojure!!

## Usage

Get the server running with Leiningen. 4000 is the port. You can then run localhost:4000 on any web browser to access the server.

    $ lein run 4000